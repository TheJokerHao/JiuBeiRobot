<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/bgColor_default" />

    <!-- padding设置内容区域离边界的间距 -->
    <padding
        android:bottom="@dimen/common_margin10dp"
        android:left="@dimen/common_margin10dp"
        android:right="@dimen/common_margin10dp"
        android:top="@dimen/common_margin10dp" />

    <!-- corners设置圆角，只适用于rectangle -->
    <corners android:radius="10dp" />

    <!-- stroke设置描边 -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray"
        android:dashGap="4dp"
        android:dashWidth="4dp" />


</shape>